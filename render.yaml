services:
  - type: web
    name: flovifly
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: JWT_SECRET
        generateValue: true
    disk:
      name: flovifly-data
      mountPath: /app/data
      sizeGB: 1
